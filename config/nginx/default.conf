  server {

    listen 80;
    # Todo: 도메인 이름 삽입
    server_name 3.36.163.92;
    location /api {
      proxy_pass http://backend:8080;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header Host $host;
      proxy_redirect off;
   }
 }